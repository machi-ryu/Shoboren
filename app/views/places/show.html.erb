<h1>場所データ</h1>

<%= link_to "場所修正", edit_place_path, class:"btn btn-primary"%>

<div class="container">
    <div class="name"><%= @place.name %></div>
    <div class="menu"><%= @place.menu %></div>
    <div class="time"><%= @place.stime.strftime("%m/%d %a %H:%M〜") %><%= @place.etime.strftime("%H:%M") %></div>
    <div class="intention_state"><%= @place.intention_state %></div>
<div>