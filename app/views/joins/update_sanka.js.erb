$("#result<%= @place.id %>").html("<%= escape_javascript(render('user/result', place: @place)) %>");
$("#interval").html("<%= escape_javascript(render('user/interval',
                           sanka_place: @sanka_place,
                           blank_term: @blank_term,
                           user: @user )) %>");